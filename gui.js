var gui = new Gui();

/*var card = Image.load("./config/ChatTriggers/modules/DailyRewards/assets/images/card_back.png")
register("renderOverlay", function() {
  var x = (Renderer.screen.getWidth() - 110) / 2 - (110 + 50)
  var y = (Renderer.screen.getHeight() - 157) / 2
    Renderer.drawImage(card, x + 0 * (110 + 50), y, 110, 157);
    Renderer.drawImage(card, x + 1 * (110 + 50), y, 110, 157);
    Renderer.drawImage(card, x + 2 * (110 + 50), y, 110, 157);
});
*/
